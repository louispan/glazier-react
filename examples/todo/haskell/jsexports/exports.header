'use strict';
// Webpack can shim this into a NodeJS module, so 'exports' and 'require' is in scope
const React = require('react');
const ReactDOM = require('react-dom');
const Dummy = require('../src/Dummy').default;
